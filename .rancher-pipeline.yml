stages:
- name: Build
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: juniper
      pushRemote: true
      registry: docker.io
timeout: 60
notification: {}
